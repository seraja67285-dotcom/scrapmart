import { prisma } from "@/lib/prisma";
export const dynamic="force-dynamic";
export default async function Admin(){const [products,categories,settings]=await Promise.all([prisma.product.findMany({include:{category:true},orderBy:{createdAt:"desc"}}),prisma.category.findMany(),prisma.siteSettings.findUnique({where:{id:"main"}})]);return <main className="container admin"><h1 style={{fontSize:32}}>Admin Dashboard</h1><p className="muted">Initial dashboard. Secure authentication and CRUD actions are the next deployment step.</p><div className="card body"><h2>Products ({products.length})</h2><table className="table"><thead><tr><th>Name</th><th>Category</th><th>Price</th><th>Status</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td>{p.name}</td><td>{p.category.name}</td><td>{p.price||"—"}</td><td>{p.available?"Available":"Hidden"}</td></tr>)}</tbody></table></div><div className="card body" style={{marginTop:18}}><h2>Settings</h2><p>WhatsApp: {settings?.whatsappNumber||"Not configured"}</p><p>Site: {settings?.siteName||"ScrapMart"}</p></div></main>}